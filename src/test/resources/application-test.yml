#spring:
#  config:
#    activate:
#      on-profile: test
#  liquibase:
#    enabled: true
#    change-log: classpath:db/changelog/db.changelog-master.yaml

#logging:
#  level:
#    liquibase: DEBUG

spring:
  r2dbc:
    pool:
      initial-size: 10
      max-size: 20
